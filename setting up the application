this project is developed using flask in windows machiene so steps for set up follows:
---
in your terminal type the following commands to setup and run the project after cloning the repository
---
1. git clone <repository_name>
2. cd <repository>
3. python -m venv ./venv/
4. \venv\Scripts\activate
5. pip install flask
6. pip freeze > requirements.txt
7. flask run
